# 다음 릴리스 변경사항

## [덮어쓰기]

### 퍼블리싱 인덱스 — 탄소 FO IA 현행 반영 (73건)

- 대상: src/content/publishing-guide/publishing-index.json
- 적용: 지정한 파일만 교체
- 변경: 탄소 FO 저장소의 퍼블리싱 인덱스를 다시 맞췄다. 선도기업 신청 3차 `목표달성 평가` 갈래가 4건 늘고(일반기업 미달성, 목표관리업체 달성·절대배출량 달성·전 기준 미달) `일반기업 · 달성` 1건이 완료로 올라갔다. 삭제된 화면은 없다
- 상태: 탄소 FO 의 `개발 수정완료` 는 우리 상태 집합에 없는 값이라 완료로 옮겨 센다 — 이번에는 `자가진단 > 평가지표 작성` 1건이 해당한다
- 링크: 완료된 화면 중 탄소 FO 에 실제로 화면이 있는 54건에만 주소를 단다. 완료 60건 가운데 다운로드용 `Link` 5건과 하위 화면만 있는 `마이페이지` 는 그쪽에도 화면이 없어 링크를 두지 않는다
- 결과: 탄소가 69건에서 73건이 되고 UIUX 진척률이 80% → 82%(60/73)로 오른다
