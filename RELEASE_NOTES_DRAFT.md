# 다음 릴리스 변경사항

## [덮어쓰기]

### 퍼블리싱 인덱스 화면 — 탄소에도 응용2 열 표시

- 대상: src/components/custom/publishing-index.tsx
- 적용: 파일 교체
- 변경: 응용2 진척률 카드·표의 응용2 열·상태 갱신 안내를 탄소 탭에서도 보여 준다. 감추는 것은 공통 레이아웃 표 하나로 줄였다 — 이 저장소가 화면을 찍어내는 틀이라 탄소에는 해당하는 것이 없다
- 이유: 탄소 화면도 응용2 상태를 기업·기관과 똑같이 이 저장소에서 관리한다. 표에 열이 없으면 어느 화면이 남았는지 볼 자리가 없다
- 사용: 응용2팀은 유형과 관계없이 같은 방법으로 고친다 — 뎁스 배지를 눌러 키를 복사하고 `publishing-index.json` 에서 그 키를 찾아 `application2Status` 를 적는다

### 퍼블리싱 인덱스 — 탄소 FO IA 현행 반영 (73건)

- 대상: src/content/publishing-guide/publishing-index.json
- 적용: 지정한 파일만 교체
- 변경: 탄소 FO 저장소의 퍼블리싱 인덱스를 다시 맞췄다. 화면 추가·삭제는 없고 `자가진단 > 인벤토리 배출량 산정` 3건(자신·항목 선택·상세 입력)이 완료로 올라갔다
- 결과: 탄소 UIUX 진척률이 82% → 86%(63/73)로 오르고 화면 주소가 54건에서 57건이 된다. 응용2는 아직 값이 없어 0%(0/73)에서 시작한다
