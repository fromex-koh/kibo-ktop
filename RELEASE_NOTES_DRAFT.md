# 다음 릴리스 변경사항

## [덮어쓰기]

### 퍼블리싱 인덱스 — 탄소 FO IA 현행 반영 (74건)

- 대상: src/content/publishing-guide/publishing-index.json
- 적용: 지정한 파일만 교체
- 변경: 탄소 FO 저장소의 퍼블리싱 인덱스를 다시 맞췄다. 화면 1건이 늘고(선도기업 신청 최초 진입의 회원 진입 모달) 선도기업 신청 2·3차 6건이 완료로 올라갔다. 삭제·경로 변경은 없다
- 상태: 탄소 FO 의 `개발 수정요청` 은 아직 손볼 것이 남은 행이라 `수정요청` 으로 옮긴다 — 완료로 세지 않는다. `개발 수정완료`(7건)는 종전대로 완료다. 이번에 `자가진단 > 감축잠재량 산정` 이 완료에서 수정요청으로 내려갔다
- 링크: 기준을 진행 상태에서 화면 유무로 바꾼다. 탄소 FO 에 화면이 있으면 상태와 무관하게 주소를 달아 65건이 된다 — 링크는 주소가 열리는지의 문제라, 수정요청이어도 열어 볼 수 있으면 여는 편이 낫다
- 결과: 탄소가 73건에서 74건이 되고 UIUX 진척률이 86% → 95%(70/74)로 오른다. 응용2는 아직 값이 없어 0%(0/74)다
